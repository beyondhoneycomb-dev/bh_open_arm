"""WP-0C-05 acceptance suite — dummy mode.

Proves the dummy follower and leader return the real schema (WP-0A-02), swap for a
real backend with no caller change, open no CAN socket, carry a fault-injection
scenario library whose six faults each provoke their upstream reaction, and import
no LeRobot in-tree loop nor any CLI-spawn symbol.
"""
